CREATE TABLE "public"."user_task" ("id" serial NOT NULL, "user_id" integer, "task_id" integer NOT NULL, "created_at" timestamptz NOT NULL, "created_by" integer NOT NULL, "status" integer NOT NULL DEFAULT 0, PRIMARY KEY ("id") , FOREIGN KEY ("user_id") REFERENCES "public"."user"("id") ON UPDATE restrict ON DELETE cascade, FOREIGN KEY ("task_id") REFERENCES "public"."tasks"("id") ON UPDATE restrict ON DELETE cascade);
